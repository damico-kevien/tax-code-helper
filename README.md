# tax-code-helper
Utility to retrieve date of birth and age from tax code

# API
### GET -> /api/v1/tax-code/{taxCode}
Extract date of birth and age from tax code

# SWAGGER
Run the application and [click here](http://localhost:8080/swagger-ui/index.html) to try!
